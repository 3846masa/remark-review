<%
var id = (() => {
  var arr = url.split('/').pop().split('.');
  arr.pop();
  return arr.join('.');
})();
-%>
//image[<%= id %>][<%= caption.replace(/\]/g, '\\]') %>][<%= config %>]{
//}
<%# Line Break %>
