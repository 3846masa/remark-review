<%
var id = (() => {
  var arr = node.url.split('/').pop().split('.');
  arr.pop();
  return arr.join('.');
})();
-%>
<%# Line Break %>
//image[<%= id %>][<%= (node.caption || '').replace(/\]/g, '\\]') %>][<%= node.utils.stringify(node.config) %>]{
//}
<%# Line Break %>
